# GUI
PySide6>=6.5

# PDF script parsing (for script verification feature)
PyMuPDF>=1.23.0

# Speech-to-text — LOCAL mode (runs Whisper on your machine, no internet needed)
# Requires ~1-10 GB disk for model weights depending on model size chosen.
# GPU (CUDA) speeds things up massively but CPU works too.
# Intel macOS needs the last CPython 3.12 wheels published for these packages.
# Without these pins, pip tries to compile LLVM/llvmlite locally and aborts the
# entire install before even PySide6 is installed. These markers do not affect
# Apple Silicon, Windows, or Linux.
numpy==1.26.4; sys_platform == "darwin" and platform_machine == "x86_64"
torch==2.2.2; sys_platform == "darwin" and platform_machine == "x86_64"
llvmlite==0.44.0; sys_platform == "darwin" and platform_machine == "x86_64"
numba==0.61.2; sys_platform == "darwin" and platform_machine == "x86_64"
openai-whisper>=20230314
indic-transliteration>=2.3.82

# Optional downloadable precision alignment packs.  The Meta MMS weights are
# not bundled in the application; only this runtime is packaged.
transformers>=4.48,<5
huggingface-hub>=0.27,<1
safetensors>=0.4
soundfile>=0.12

# Speech-to-text — API mode (OpenAI Whisper API, requires internet + API key)
# Only needed if you use mode="api" in settings; skip if using local-only.
openai>=1.0.0

# audioop was removed in Python 3.13+; this backport restores it
audioop-lts; python_version >= "3.13"

# Mastering (studio-grade audio processing, Logic Pro quality)
pedalboard>=0.9.24
pyloudnorm>=0.1.0
numpy>=1.20.0
scipy>=1.7.0

# NOTE: loudness + true-peak measurement is done by calling ffmpeg (a separate
# program), NOT a Python package. Install ffmpeg separately — see README.
# Everything else (WAV marker reading, silence detection, the Bible database)
# uses only the Python standard library.
